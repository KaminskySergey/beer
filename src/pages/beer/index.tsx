import GalleryList from "@/components/gallery/GalleryList"
import Layout from "@/components/layout/Layout"

const Beer = () => {
    return (
        <Layout>
            <GalleryList />
        </Layout>
    )
}

export default Beer